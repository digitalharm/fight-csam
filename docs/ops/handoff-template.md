# Handoff Template

Use this at the end of every work session or before handing a worktree to another agent.

```text
Date:
Agent:
Track:
Worktree path:
Branch:

Goal for this session:

Files changed:

Tests run:

Safety guard run:

What passed:

What failed:

Safety-sensitive decisions:

Known blockers:

Next concrete step:
```

