v0.5 — Wave 1: real PDQ implemented via the upstream pdqhash crate (Apache-2.0). hash_from_luma + hash_dihedral_from_luma working; 7 tests pass (shape, determinism, dim/buffer rejection, 8-dihedral). Next: conformance corpus at vectors/ with CI drift-gating, then native↔WASM byte-identical proof (v1.0).
