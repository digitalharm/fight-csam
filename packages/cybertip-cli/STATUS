v0.5 — Wave 4: data model + validation + dry-run wire payload + SANDBOX simulation path (validate + build payload + emit the curl-equivalent for an operator-supplied NCMEC_SANDBOX_URL; performs NO real network I/O). Both Node (16 tests pass) and Python (17 tests pass). Production submit path BLOCKED at the CLI (ProductionSubmitBlocked / counsel-required error) until outside counsel reviews docs/counsel-scope-brief.md. Next: real sandbox POST once ESP credentials + counsel sign-off land (v1.0).
