v0.5 — Wave 4: data model + validation + dry-run wire payload + SANDBOX simulation path (validate + build payload + emit the curl-equivalent for an operator-supplied NCMEC_SANDBOX_URL; performs NO real network I/O — the operator runs the emitted request themselves, and the Authorization header is always an <ESP_CREDENTIAL> placeholder). Both Node (15 tests pass) and Python (16 tests pass); tsc + ruff clean. Production submit path is ABSENT by design (ProductionSubmitBlocked / counsel-required error at the CLI) until outside counsel reviews docs/counsel-scope-brief.md. Next: a real sandbox POST once ESP credentials + counsel sign-off land (v1.0).
