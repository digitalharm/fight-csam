v0.5 — Wave 4: HTTP API (full package lifecycle) + disk persistence backend + KMS interface (noop default). Go service; build/vet/test pass. Retention schedules queryable but unenforced pending counsel review; production submission paths remain counsel-gated. Next: outside counsel review (v1.0).
