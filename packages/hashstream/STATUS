Alpha (operator-supplied) — Go service (HTTP API + in-memory store + adapter scaffolds for NCMEC/IWF/Project Arachnid) + TypeScript SDK. v0.5: operator-supplied hash lists work end-to-end (POST hash file -> snapshot stored -> GET serves it -> diff returns the exact delta) via a new "local" source, plus tamper-evident Ed25519 signed snapshot manifests with an operator-supplied key (verifiable from both Go and the TS SDK). Credentialed NCMEC/IWF/Project Arachnid sync remains out of scope, awaiting list access.
